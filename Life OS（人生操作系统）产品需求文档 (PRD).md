
|**产品名称**|**Life OS (人生操作系统)**|**当前版本**|**v1.0 (正式评审版)**|
|---|---|---|---|
|**创建日期**|2026-05-24|软件类型|移动端 App (iOS / Android)|
|**产品经理**|Gemini (AI Collaborator)|文档状态|待评审定稿|

## 1. 产品概述与全局设计规范

### 1.1 产品背景与核心定位

Life OS 是一款将“量化自我（Quantified Self）”**与**“电子宠物养成（Tamagotchi）”深度结合的个人全维管理系统。它打破了传统工具（记账、待办、健康追踪）各自为政的数据孤岛状态，将用户的真实身体代谢（BMR）、财务流转、时间执行力等数据，转化为一只虚拟宠物的生命体征与居住环境。通过情感羁绊与理性均摊机制，驱动用户经营好自己的人生。

### 1.2 全局页面栈路由与安全返回矩阵

为了确保多级嵌套下钻中拥有极佳的顺畅体验，全局路由必须严格遵循下述页面栈（Navigation Stack）控制矩阵：

|**页面层级**|**导航栏状态**|**物理返回键 / 右滑手势响应机制**|**交互与视觉规范**|
|---|---|---|---|
|**一级页面**<br><br>  <br><br>(Tab 首页大盘)|**常驻高亮**|不退出 App，默认触发系统底层**挂起应用至后台**状态。|页面间横向平滑滑动切换，无白屏。|
|**二级页面**<br><br>  <br><br>(功能聚焦工作台)|**常驻高亮**|**出栈销毁**，当前页面向右平滑滑出，上一级主大盘页面向左平滑恢复。|保持滚动位置记忆（Scroll Retention），禁止页面重新白屏刷新。|
|**三级页面**<br><br>  <br><br>(原子输入/沙盒)|**完全隐藏**|**拦截物理返回键**。如果表单被修改，强弹窗提示：`“内容未保存，确定退出吗？”`。|采用全屏右侧滑入或底部半屏 BottomSheet 抽屉拉起。|

### 1.3 模块全局色系规范

全局采用严格的模块色彩心理学规范，以在视觉上强化模块属性：

- **主品牌色**：靛蓝 (`#2B5CE6`) —— 传达系统性、理智与井井有条。
    
- **虚拟宠物**：紫色 (`#8B5CF6`) —— 活泼、具备情感温度与生命力。
    
- **财务模块**：琥珀金 (`#D97706`) —— 财富积累、警惕与掌控。
    
- **日常行动**：绯红 (`#DC2626`) —— 时间流逝与高执行力。
    
- **健康量化**：翡翠绿 (`#059669`) —— 有机、生命迹象与健康。
    

## ## 2. 系统核心功能矩阵与验收标准（FR & AC）

### 🐾 2.1 模块一：`[主页🐾]` 虚拟宠物与房间沙盒系统

#### 2.1.1 功能列表与详细说明

##### 1. 右上角健康状态微胶囊 (Status Capsule)

- **功能描述**：常驻于主页右上角的胶囊组件，展示宠物当前整体状态。
    
- **交互逻辑**：点击胶囊，原位平滑向下弹出一个半透明面板（体征仪表盘），展示水分、活力、心情及体形的具体得分与 7 日走势。
    
- **数值映射**：
    
    - **幸福 ⭐**（亮金 `#F59E0B`）：四维属性均 $\ge 70$。宠物全身散发金色粒子动效，点击触发“比心”特殊动作。
        
    - **正常 😊**（安全绿 `#10B981`）：2~3 项指标达标。宠物处于常规闲置（Idle）呼吸动画。
        
    - **疲惫 😔**（警告黄 `#F59E0B`）：活力或心情 $< 40$。宠物眼神半闭，低头趴卧。触发轻系统推送。
        
    - **不适 🤒**（严重红 `#EF4444`）：2 项或以上严重不达标（$< 30$）。宠物额头高亮红晕病态动效，发出提示音。强系统推送。
        
    - **危急 🆘**（红黑闪烁）：连续 3 天全属性 $< 20$。全屏转为黑白灰色滤镜，宠物闭眼躺在病床上。触发锁屏级高亮预警。
        

##### 2. 首页快捷行动操作轨 (Quick Action Tracker)

- **功能描述**：位于宠物房间正下方的黄金操作轨，包含四个圆形按钮（投喂、喝水、运动、休息）。
    
- **交互逻辑**：点击任意按钮，从屏幕底部唤起半屏抽屉（BottomSheet），不覆盖底部导航栏。
    

##### 3. 房间沙盒装修模式 (Sandbox Customization)

- **功能描述**：允许用户自由布设由「固定资产库」投射进房间的虚拟家具。
    
- **交互逻辑**：点击中左方 `[进入装修模式 🛠️]`，进入编辑状态。支持单指无级拖拽、双指捏合缩放（$50\% \sim 200\%$）。右上角带 `✖`（隐藏）与 `🔄`（图层 z_index 切换）。
    

##### 4. 饮食拍照与文本双通道投喂组件 (FEED Drawer)

- **功能描述**：快捷录入饮食。支持相机拍摄食物照片，通过本地端侧 AI 视觉算法自动估算卡路里并填入，同时支持手动文本搜索内置 5000+ 条开源食物库。
    

##### 5. 纯水与饮料分流蓄水组件 (DRINK Drawer)

- **功能描述**：快捷录入饮水。提供 250ml、350ml、500ml 快捷杯量及滑块微调。
    
- **核心逻辑**：分流勾选：
    
    - “纯水/淡大麦茶”：热量加算 = 0，仅转化为宠物**水分值**。
        
    - “含糖奶茶/饮料”：按固定系数 $0.45\text{ kcal/ml}$ 自动换算热量（例：500ml 奶茶 = 225kcal），同步计入今日热量摄入总池，并联动扣减财务可用预算。
        

##### 6. 活力运动组件 (SPORT Drawer)

- **功能描述**：用户输入运动类型（慢跑/骑行/游泳/力量）与时长。系统自动调取该运动的 MET 指数与用户最新体重，自动换算消耗热量：$\text{消耗} = \text{MET} \times \text{体重(kg)} \times (\text{时长}/60)$。
    

##### 7. 睡眠时间轴登记组件 (REST Drawer)

- **功能描述**：滚动滑轮登记昨晚入睡与今天起床时间，自动计算跨天时间差 $\Delta t$ 作为绝对睡眠时长，结合 1-5 星主观质量评分，修正宠物今日初始情绪。
    

#### 2.1.2 明确的验收标准 (Acceptance Criteria)

```
用例 1：测试饮水极速打卡与状态机响应
 Given 用户在首页点击 (💧) 喝水按钮
 When 选择“便携杯 350ml”，勾选“纯水/淡大麦茶”，并点击“确定”
 Then 1. 抽屉在 150ms 内无缝收回
      2. 首页右上角胶囊面板的水分值即时回升: H_new = H_old + (350 / 2000) * 100
      3. 宠物触发 2 秒的“畅饮”骨骼动画，且不增加任何今日卡路里摄入数值

用例 2：测试沙盒装扮的坐标持久化保存
 Given 用户点击 [进入装修模式 🛠️] 按钮
 When 1. 单指将[吉他模型]从坐标 (0,0) 拖拽至房间右上角 (300, 500)
      2. 双指捏合将其放大至原图的 1.50 倍
      3. 点击右上角 [💾 保存] 按钮并彻底杀死 App 进程重新冷启动
 Then 1. App 重新进入后，吉他模型完美停留在 (300, 500) 处
      4. 缩放比例稳定保持在 1.50x，未发生任何坐标漂移或大小回弹

用例 3：测试含糖饮料的热量穿透干预
 Given 用户点击 (💧) 喝水按钮
 When 选择 “运动壶 500ml”，勾选“含糖奶茶/碳酸饮料”并确定
 Then 1. 宠物水分值按比例正常回升
      2. 系统后台今日卡路里摄入总池立刻静默累加: 500 * 0.45 = 225 kcal
      3. 若该行为连续触发 3 天，触发结算事务使宠物形变值增加 10 点，模型横向拉伸变圆润
```

### 💰 2.2 模块二：`[财务💰]` 财务管理子系统

#### 2.2.1 功能列表与详细说明

##### 1. 动态净资产看板 (Net Worth Dashboard)

- **功能描述**：一级财务首页核心看板。实时汇总现金流账户余额、固定资产实时估值，对冲扣减信用卡及花呗欠款，公式：$\text{动态净资产} = \sum(\text{现金流账户}) + \sum(\text{固定资产库}) - \sum(\text{负债账户})$。
    

##### 2. 今日花费详情下钻页 (Daily Expense View)

- **功能描述**：点击大盘“今日花费”切入二级页面。由新到旧倒序排列今日流水明细。底部常驻 `[➕ 点击记一笔]`。
    

##### 3. 10秒极速记账抽屉 (Quick Ledger Drawer)

- **功能描述**：二级页面点击 `[➕ 点击记一笔]` 呼出三级抽屉。全数字键盘自动聚焦。
    
- **快捷键交互**：支持**输入金额 $\rightarrow$ 双击分类九宫格图标**快捷键，系统直接判定保存并自动收回抽屉，全流程控制在 3 次点击内。
    

##### 4. 智能固定资产库 (Smart Asset Asset Log)

- **功能描述**：记录价格与购入时间。引入**平均每日持有成本 (ADC, Average Daily Cost)** 算法：
    
    $$\text{ADC} = \frac{\text{资产购入价格}}{(\text{当前日期} - \text{购入日期}) + 1}$$
    
    该数值在资产卡片外显，每天凌晨 `00:00` 自动递减并配有绿色向下箭头（⬇️）。
    
- **资产具象化联动**：卡片提供`[在宠物房间作为家具实体化展示]`开关，开启后自动向主页沙盒派发对应 2.5D 插画模型。
    

##### 5. 自动化订阅管理 (Subscription Engine)

- **功能描述**：专门管理跨平台周期性续费（如 iCloud, Netflix）。支持输入金额、扣款账户、计费周期。
    
- **T-3 预警机制**：在“下次扣款日”前 3 天早上 09:00，强制触发系统本地通知栏续费强提醒。跨过扣费日零点时，系统后台自动在流水大表追加一笔支出，自动扣减账户余额。
    

#### 2.2.2 明确的验收标准 (Acceptance Criteria)

```
用例 1：测试双击分类极速记账手势
 Given 用户处于今日花费二级明细页，点击 [➕ 点击记一笔]
 When 键盘输入数字 “45.00”，随后对九宫格中的 [🍱 三餐] 图标执行快速双击
 Then 1. 记账抽屉在 150ms 内完全收回
      2. 自动产生一笔 45.00 元的微信支出流水
      3. 二级页面的今日总花费即时从 ￥45.00 跳跃变更为 ￥90.00

用例 2：测试固定资产均摊成本（ADC）的跨天递减
 Given 资产库中存在一项今天新录入的数码产品，购入价格为 ￥3000.00
 When 模拟时间流逝，跨过深夜 00:00 进入购入后的第二天
 Then 1. 打开固定资产二级列表页，该资产卡片上的 ADC 数字从 ￥3000.00 变更为 ￥1500.00
      2. 数字右侧正常渲染绿色向下趋势箭头（⬇️）

用例 3：测试订阅管理的 T-3 本地时钟强排程
 Given 用户登记一项续费日为 2026-06-01 的 Netflix 订阅，开启 T-3 强提醒
 When 系统时间推进到 2026-05-29 早上 09:00:00
 Then 1. 即使应用处于后台彻底杀死状态，系统通知栏必须准时弹窗强预警
      2. 弹窗明文提示扣费金额（￥98.00）与预备扣款账户
```

### 📊 2.3 模块三：`[分析📊]` 数据复盘与交叉洞察中枢

#### 2.3.1 功能列表与详细说明

##### 1. AI 跨模块智能生活洞察卡片 (Insight Generator)

- **功能描述**：分析大盘首页的核心高亮卡片。提取用户过去 21 天的睡眠、运动打卡等自律数组，与次日特定冲动开销分类金额进行**皮尔逊相关系数 ($r$) 离线异步计算**。
    
- **生成阈值**：当计算得出 相关系数 $|r| \ge 0.65$（强相关）时，自动组装生成前台看到的“生活真相洞察卡片”（例如：“深夜熬夜导致次日冲动点外卖超支上升 42%”）。点击卡片可下钻进入二级因果日志，查看双轴对照走势图。
    

##### 2. 财务收支深度拆解分析页 (Financial Breakdown View)

- **功能描述**：分析首页点击财务区域进入。以直观的色彩条形图横向拆解本月支出分类占比（如：三餐 40%、购物 25%）与渠道消耗分布。
    
- **预算爆舱情感化阻断**：当某一分类实际支出超支达 100% 时，底部高亮浮现爆舱警告，结合宠物伤心动效与碎裂存钱罐，从情感层阻断冲动消费。点击分类条形图可进一步下钻进入【三级：单分类月度流水明细页】进行精准捉鬼。
    

##### 3. 日常效率与执行力深度分析页 (Executive Score Panel)

- **功能描述**：分析首页点击日常效率区域进入二级页。展示动态加权换算得出的“时间掌控指数”。
    
- **习惯连击墙 (Streaks Matrix)**：以横向滑动的习惯胶囊流展示达标率，连击天数 $> 7$ 天时自动点亮**闪烁小火焰动效** 🔥。点击条目可下钻进入【三级：365天全景格子热力图页】。
    

##### 4. 日历账单回溯明细矩阵 (Calendar Ledger Map)

- **功能描述**：分析首页点击右上角 `[日历]` 按钮进入二级页面。中部位 5×7 的标准月度日历网格热力图，每个日期方块内直接外显当天总支出数字。
    
- **压力红点机制**：单日开销 $\le 50$ 元浅灰色；$51 \sim 300$ 元常态深色；$> 300$ 元自动变为**警告红**。点击任意日期，下方组件以 **Lazy Loading（懒加载）** 形式零延迟无缝刷新该日所有的花费流水明细。
    

#### 2.3.2 明确的验收标准 (Acceptance Criteria)

```
用例 1：测试日历账单回溯的懒加载刷新速度
 Given 用户处于日历账单回溯二级页，当前日历处于 5 月份
 When 用户点击日历网格中的 “5月7日” (该日包含 15 条高频流水)
 Then 下方消费明细列表在 100ms 内无白屏、无闪烁刷新完成，按从新到旧精确吐出 15 条对账数据

用例 2：测试预算爆舱卡片的情感化触发
 Given 用户本月[🎉 娱乐]分类设定预算为 500 元，当前实际已用 490 元
 When 用户通过记账追加一笔 20 元的密室逃脱费用，使总流出达到 510 元 (超支)
 Then 打开二级财务分析页，底部瞬间高亮浮现裂开的存钱罐警告组件，宠物动画重设为伤心流泪

用例 3：测试三级习惯热力图的边界数据隔离
 Given 用户处于[📖 每晚读书20页]的 365 天三景格子网格中
 When 轻触点击历史格子中的 “2026-03-15” (该日用户处于度假模式)
 Then 1. 格子上方精准浮现 Tooltip 提示悬浮窗
      2. 悬浮窗明文标注：“2026-03-15 开启度假模式，状态已冻结，不计入断签”
```

### 📆 2.4 模块四：`[日常📆]` 每日行动与时间管理工作台

#### 2.4.1 功能列表与详细说明

##### 1. 全局闪电收件箱 (Inbox Instant Input)

- **功能描述**：日常首页顶部的盲录输入框。支持单手闪电录入一句简短文本（如“18:00 去健身房”），点击回车即刻创建任务。
    

##### 2. 四象限待办控制台 (Quadrant Todo Engine)

- **功能描述**：将日常首页核心区域划分为 2×2 的四象限网格：🔴 A.重要紧急 / 🟡 B.重要不紧急 / 🔵 C.紧急不重要 / 🟢 D.不重要不紧急。
    
- **手势控制**：支持长按任务 0.5 秒激活悬浮拖拽（Drag-and-Drop）手势，用户可直接单指将任务卡片在四个网格间进行划归分类，系统后台秒级改写优先级。点击任务可进一步下钻进入【三级：任务高级属性加工面板】进行子任务步骤拆解。
    

##### 3. Rollover 零阻断跨天自动滚动任务系统

- **功能描述**：每日深夜 `00:00:00`，后台 Worker 执行无网络本地自动化流转事务。所有未完成的任务，其 `target_date` 自动推进修改为 `TOMORROW`（明天），且任务卡片上的 `delay_count`（延期计数）自增 $+1$。在前台对应卡片左侧打上显眼的红色 `[延期 X 天]` 标签。
    

##### 4. 历史复盘日志日历库与沙盒编辑 Studio

- **功能描述**：日常首页最下方点击引导卡进入二级复盘索引库，点击 `[开启今日复盘]` 进入三级全屏沉浸式 Studio。
    
- **全自动数据拼装机制**：三级复盘 Studio 顶轨由系统自动抓取并拼装今日的只读生活简报：包含今日花费总计明细、今日已终结任务、今日延期未完任务。下方拉起结构化文本键盘供用户书写反思心得。
    
- **心情晴雨表联动**：复盘保存时用户必须单选今日心情标签（😄/😐/😔/😡/🤒），该标签以高权重直接改写明早宠物醒来时的初始情绪体征值。
    

#### 2.4.2 明确的验收标准 (Acceptance Criteria)

```
用例 1：测试四象限长按拖拽分类手势
 Given 用户处于日常管理首页，A 象限内存在一条“签署客户提案”任务
 When 用户按住该任务卡片持续达 0.5 秒，卡片发生缩放悬浮反馈，将其单指拖入 B 象限网格并松手
 Then 1. 任务卡片完美吸附嵌入 B 象限列表
      2. 本地数据库中该任务的 `priority_quadrant` 字段在 50ms 内静默改写为 'B'

用例 2：测试深夜 Worker 任务滚动与宠物惩罚加算
 Given 系统内存在 2 个未勾选的 A 象限高优待办任务，当前系统时间为 23:59:59
 When 时间向前推进一秒，跨越 00:00:00 触发 Midnight 自动化 Worker
 Then 1. 两个待办任务的执行日期属性自动变更为新的一天，任务未被销毁
      2. 任务的 `delay_count` 变为 1
      3. 跨模块通信激活，主页宠物的初始心情值在结算的一瞬间被强行硬扣减 15 点

用例 3：测试三级复盘 Studio 的数据防穿透死快照
 Given 用户于 5 月 24 日晚上进行今日复盘，系统自动抓取只读简报显示“今日花费：￥45.00”
 When 用户保存复盘后，在未来 6 月份回溯删除了 5 月 24 日的一笔 15 元财务流水
 Then 重新进入 5 月 24 日的三级历史复盘日志页，文档上半部分的简报依然锁死显示“今日花费：￥45.00”，不受历史表数据变更的穿透破坏
```

### 👤 2.5 模块五：`[我的👤]` 个人数字档案系统与隐私金库

#### 2.5.1 功能列表与详细说明

##### 1. 极客技术画像中心 (Geek Profile Core)

- **功能描述**：展示用户的数字化分身昵称、格言（Motto）以及横向排布的技术生活标签。点击区域下钻进入【三级：技术标签与履历编辑器】，支持轻量级 Markdown 文本编写长期人生规划。
    

##### 2. 安全证件资产库 (Secure Crypt Vault)

- **功能描述**：二级聚焦工作台。专门用来管理护照、身份证、驾驶证等高敏数字资产。
    
- **物理阻断防线 (Gatekeeper Lock)**：从一级点击进入此页时，**路由拦截器强行挂起，必须拉起原生 Face ID / 指纹进行生物层级校验**，验证失败直接弹回一级，防范手机解锁状态下的二次隐私泄露。
    
- **三级 OCR 加密入库**：点击 `[➕ 扫描并添加新证件]` 进入三级页，启动本地安全相机扫描，端侧自动运行 OCR 提取证件号与到期日。证件号使用 **AES-256 硬件级芯片密文二进制块（BLOB）** 存储，严禁明文进库。
    

##### 3. 农/公历周期纪念日看板 (Anniversary Timeline)

- **功能描述**：时间轴滚动管理重要日子（如妈妈生日、结婚纪念日）。支持农历（阴历）周期性自动对齐换算。
    
- **跨模块数据总线 (Cross-Module Bus)**：点击条目进入三级配置页，可开启深度智能联动：
    
    - _时间总线_：设定提前 7 天自动在 `[日常📆]` 模块的 A 象限（重要紧急）里无感派发一条“挑选生日礼物”的任务。
        
    - _财务总线_：设定礼物预留基金（如 1500 元），系统将在到期日前 15 天自动在 `[财务💰]` 的可用月度预算大盘中强制锁死这笔资金，将其标记为“不可用预留储蓄金”，对冲超支风险。
        

##### 4. 核心人际网络与交往温度计 (Relationship Network)

- **功能描述**：二级页面。记录至多 6 位核心生命圈子亲友。系统内置“危机不联络阈值”（默认 14 天）。
    
- **温度计衰减机制**：若超过 14 天在财务没有与其产生的 AA 制记账流水、且在日常复盘没有提及此人，亲友卡片的互动热度会跌破阈值并触发红色危机警报 `[去联络]`。点击进入三级表单可录入“人际交往日志”，保存后热度瞬间反弹重置。
    

#### 2.5.2 明确的验收标准 (Acceptance Criteria)

```
用例 1：测试证件资产库的 Face ID 路由物理拦截
 Given 用户处于 [我的👤] 一级大盘主页，手机当前处于正常解锁状态
 When 用户指尖点击 [📁 证件资产库] 这一二级入口区域
 Then 1. 前端路由平滑右滑动画被强行阻断挂起，屏幕中央强制唤起 OS 原生 Face ID 生物校验弹窗
      2. 故意遮挡摄像头使 Face ID 识别失败 3 次，页面不进入二级证件库，安全退回一级主页

用例 2：测试阴历纪念日向日常模块自动派发待办
 Given 个人档案中存在一项绑定农历四月初八的【妈妈生日】纪念日，设定提前 7 天联动
 When 随着系统时间推进，当前公历日期到达该年农历四月初一的凌晨 00:01
 Then 1. 日常模块总线捕获事件
      2. 自动在 [日常📆] 首页的 🔴 A象限(重要紧急)网格中自动置顶生成一条标题为“挑选妈妈生日礼物”的闪电任务，无需用户手动新建

用例 3：测试纪念日对财务可用预算的强前置锁定
 Given 某项纪念日设定了 ￥1500.00 元的跨模块礼物预算，设定提前 15 天锁定
 When 系统时间轴推进到距离纪念日正日还有 15 天的清晨 06:00
 Then 打开 [财务💰] 看板首页，本月可用分类预算总大盘自动静默扣减 1500 元，进度条向橙色警告段逼近，并在预算详情中标注“纪念日专项冻结储备金：￥1500”
```

## ## 3. 系统底层数据库实体关系模型 (Database Schemas)

为了确保全工具链在完全断网（如机舱、地下室）环境下仍能 100% 达成数据原子性写入与本地运算，本地客户端 SQLite 持久化层必须部署以下核心加密数据表结构。

### 3.1 虚拟宠物核心体征资产状态表 (`pet_status_core`)

SQL

```
CREATE TABLE pet_status_core (
    pet_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    pet_name VARCHAR(50) DEFAULT '小生活',
    species_type TEXT CHECK(species_type IN ('CAT', 'DOG', 'RABBIT', 'DRAGON')),
    growth_stage TEXT CHECK(growth_stage IN ('EGG', 'BABY', 'TEEN', 'ADULT', 'LEGEND')),
    hydration_points INTEGER DEFAULT 100,       -- 水分值 (0-100)
    body_shape_points INTEGER DEFAULT 0,        -- 体形值 (-50 到 50)
    energy_points INTEGER DEFAULT 100,          -- 活力值 (0-100)
    mood_points INTEGER DEFAULT 100,            -- 心情值 (0-100)
    overall_status_level TEXT DEFAULT 'NORMAL', -- 幸福/正常/疲惫/不适/危急
    accumulated_days INTEGER DEFAULT 0,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 3.2 财务流水明细大表 (`financial_transaction`)

SQL

```
CREATE TABLE financial_transaction (
    transaction_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    flow_type TEXT CHECK(flow_type IN ('INCOME', 'EXPENSE', 'TRANSFER')),
    amount DECIMAL(12, 2) NOT NULL,
    category_id VARCHAR(50) NOT NULL,           -- 三餐/交通/娱乐/自定义等
    account_id VARCHAR(50) NOT NULL,            -- 微信/支付宝/银行卡/花呗
    remark VARCHAR(150),
    logged_at TIMESTAMP NOT NULL
);
```

### 3.3 待办清单主干执行表 (`todo_execution_list`)

SQL

```
CREATE TABLE todo_execution_list (
    todo_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    title VARCHAR(150) NOT NULL,
    priority_quadrant TEXT CHECK(priority_quadrant IN ('A', 'B', 'C', 'D')),
    target_date DATE NOT NULL,
    reminder_clock TIMESTAMP,
    is_completed INTEGER DEFAULT 0,              -- 0:未完成, 1:已完成
    delay_count INTEGER DEFAULT 0,               -- 凌晨Worker滚动的累加延期天数
    associated_flag_id VARCHAR(36),              -- 可选外键：关联长期大目标
    completed_at TIMESTAMP                       -- 精确完成时间戳，交由分析模块进行高效时段挖掘
);
```

### 3.4 高敏感隐私证件端侧全隔离加密表 (`secure_documents_vault`)

SQL

```
CREATE TABLE secure_documents_vault (
    doc_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    doc_type TEXT CHECK(doc_type IN ('PASSPORT', 'ID_CARD', 'DRIVER_LICENSE', 'OTHER')),
    encrypted_number_blob BLOB NOT NULL,         -- 硬件芯片加密级别的二进制文件块，拒绝明文防脱敏篡改
    expiry_date DATE NOT NULL,
    alert_offset_days INTEGER DEFAULT 30,
    requires_face_id_secondary INTEGER DEFAULT 1,-- 1:强启动Gatekeeper物理二次拦截
    local_only_island_flag INTEGER DEFAULT 1,    -- 1:严格本地隔离，多端云备份时直接剔除此行防泄露
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## ## 4. 项目里程碑规划与发版排期 (Roadmap)

本期项目架构庞大、联动逻辑极深，全案产品生命周期总开发周期精确控制在 **19 周**，各研发迭代阶段核心产出规划如下：

```
【Phase 0: 准备与技术选型】 (第 1-2 周)
 └── 关键产出：本 PRD 通盘方案评审定稿、本地端侧 SQLCipher 加密库压力测试报告。

【Phase 1: 视觉打毛坯与交互稿】 (第 3-5 周)
 └── 关键产出：Figma 5主Tab三级联动交互高保真视觉稿、Rive 宠物 2.5D 模型动态骨骼绑定。

【Phase 2: Sprint 1 量化基础底座】 (第 6-9 周)
 └── 关键产出：日常花费记账、四象限快捷 Todo、投喂/喝水等四大首页操作轨抽屉核心表结构调通。

【Phase 3: Sprint 2 状态机与沙盒装扮】 (第 10-13 周)
 └── 关键产出：宠物四维体征 00:00 缓动结算 Worker、沙盒网格吸附与双指捏合缩放 Canvas 场景开发完毕。

【Phase 4: Sprint 3 智能中枢与后方金库】 (第 14-16 周)
 └── 关键产出：离线皮尔逊因果挖掘引擎、日历账单压力红点热力图、Face ID 拦截器与跨模块时间/财务总线。

【Phase 5: 全面集成联调与压测】 (第 17-18 周)
 └── 关键产出：闭门内测 Bug 修复、确保低端机型常驻动效帧率稳定在 >= 45fps、交付全量测试报告。

【Phase 6: 正式商业化发版】 (第 19 周)
 └── 关键产出：App Store / Google Play 伴随《免责与非医疗趣味诊断激励合规声明》正式公开上架。
```

> **文档变更记录说明**
> 
> 任何团队成员如因业务变更修改本文档，请严格在顶部追加版本迭代号并记录于此。
> 
> - `v1.0 (2026-05-24)`: 初始版本评审定稿。全面收纳打通个人生活量化数据、电子宠物四维缓动状态机与固定资产房间沙盒自定义三大核心模块，进入全面开发排期。
>